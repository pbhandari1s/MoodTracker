@model MoodTracker.Entities.ViewModel.ActivityTypeViewModel

@{
    ViewBag.Title = "Unarchive";
}

@using (Html.BeginForm("unarchive", "activitytype", FormMethod.Post, new { @class="form-big"}))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.Id)
    @Html.HiddenFor(model => model.Type)
    <div class="form-horizontal">
        <div class="pageHeading">
            <h2>Activity Type</h2>
            <p>
                Are you sure you want to unarchive this?
            </p>
        </div>

        <div class="row form">
            <div class="col-lg-12">
                <div>
                    <u>@Html.Label("Activity Type")</u>
                </div>
                @Html.Label(Model.Type)
            </div>
        </div>

        <div class="form-action">
            <input type="submit" value="Unarchive" class="btn btn-primary btn-lg" />
            @Html.ActionLink("Cancel", "index", "activitytype", null, new { @class = "btn btn-secondary btn-lg" })
        </div>


        <div class="backAction">
            @Html.ActionLink("<< Back to Current List", "Index")
        </div>
    </div>
}