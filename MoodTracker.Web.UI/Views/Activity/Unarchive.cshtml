@model MoodTracker.Entities.ViewModel.ActivityBasicInfoViewModel

@{
    ViewBag.Title = "Unarchive";
}

@using (Html.BeginForm("unarchive", "activity", FormMethod.Post, new { @class="form-big"}))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.Id)
    <div class="form-horizontal">
        <div class="pageHeading">
            <h2>Activity</h2>
            <p>
                Are you sure you want to unarchive this?
            </p>
        </div>

        <div class="row form">
            <div class="col-lg-12">
                <div>
                    <u>@Html.LabelFor(model => model.Title)</u>
                </div>
                @Html.Label(Model.Title)
            </div>
        </div>

        <div class="form-action">
            <input type="submit" value="Unarchive" class="btn btn-primary btn-lg" />
            @Html.ActionLink("Cancel", "index", "activity", null, new { @class = "btn btn-secondary btn-lg" })
        </div>

        <div class="backAction">
            @Html.ActionLink("<< Back to Current List", "Index")
        </div>
    </div>
}